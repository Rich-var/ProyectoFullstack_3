server:
  port: 8083

spring:
  application:
    name: AUTH-SERVICE
  profiles:
    active: dev

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
  enable-hateoas: false
